server:
  port: 8080
  servlet:
    context-path: /api
mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: github.runoob09.usercenter.mybaits.entity
mybatis-plus:
  global-config:
    db-config:
      id-type: ASSIGN_ID
